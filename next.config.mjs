/** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;

const nextConfig = {
  images: {
    unoptimized: true,
    domains: ['m.media-amazon.com']
  }
};

export default nextConfig;